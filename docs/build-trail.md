# Versioned PRDs — build trail

Historical reference for how the codebase got here. Most of the load-bearing rationale also lives in `decisions/` as ADRs. Load this file when you need long-form context for a specific version; otherwise CLAUDE.md's decision-log section and the ADR index are usually enough.

- **v1.2** — baseline: auth, onboarding, subscription, i18n
- **v1.3** — WAC, sales module, khata view enhancements
- **v1.4** — partial payments, customer expansion, walk-ins
- **v1.5** — product type uniqueness, opening stock, fuzzy search, POS redesign
- **v1.6** — ledger hardening: stored balance, reversals, occurred_at, append-only
- **v1.7** — design system revamp (mint→navy palette, Urdu font fix, DataTable primitive)
- **v1.8** — db hardening: RLS audit, search_path, FK indexes, locks, money precision, view security_invoker, append-only across financial tables
- **v1.9** — stock-in hardening: suppliers, landed-cost pro-rata allocation, bidirectional cost calc, searchable product/supplier comboboxes, MTD-default list with pagination, Effect-on-Inventory snapshot fix
- **v2.0** — *superseded by v2.1*. UoM + first-class pack pricing was spec'd and partially built (migrations 0026/0027), then walked back. `MVP_v2.0_UNITS_OF_MEASURE.md` is historical reference only; do not implement against it.
- **v2.1** — stock-in unit handling: `units_of_measure`, `product_packs` (no price column — packs are stock-in shortcuts only), `products.is_scan_only`, `purchase_items.{pack_id, pack_qty, pack_base_qty_snapshot, qty_in_base}`. Sales stay in base units; `record_sale` byte-equivalent to v1.6. Stock display view exposes `whole_packs` + `remainder_base` directly so the frontend never recomputes breakdowns. `record_purchase` accepts pack-shaped lines; `define_pack_inline` auto-creates UoMs at stock-in time. POS quick-add buttons; scan-only enforcement.
- **v2.2** — customer tiers + per-line discounts + manual override. `customer_tiers` (named %-only groups, default per shop), `customers.tier_id`, invoice snapshot of tier id/percent/amount/override-type/override-value, `sale_items.line_discount_*`. `record_sale` rewrites: stacking order is **negotiated price → per-line discount → tier (or override) → service charge added last**. Discounts apply to items only, never service. *Partially reverted in v2.3 — see below.*
- **v2.3** — fixes from v2.1/v2.2 testing. (a) Tiers reduced to pure customer categories: `customer_tiers.discount_percent` dropped; tiers no longer auto-discount sales. The popup is now the only source of an invoice-level discount. (b) Invoice columns renamed `tier_*` → `sale_discount_*` (4 columns + recreated `invoices_sale_discount_consistent` constraint). (c) Overhead allocation rewritten: stored as new `purchase_items.line_overhead_amount` via **largest-remainder** method in `record_purchase` (overhead distributed pro-rata by line value, leftover penny goes to the highest-value line). The legacy `overhead_per_unit` column is kept for back-compat — drop in a future cleanup. (d) `search_products` is name-only (combined name+type GIN index dropped; relevance preserved + `p_only_in_stock` filter). (e) Frontend: PurchaseDetail row indices precomputed (DataTable's `cell` doesn't pass index — was rendering NaN); columns restructured to # / Product / Qty / Unit cost / Subtotal / Overhead / Total with explanatory note; "Cost change" replaces "Δ"; per-piece cost shown under unit cost when pack base_qty > 1; supplier dropdown widened. (f) POS: tier auto-discount logic removed; customer card with tier chip pinned at the top of the cart; cart column headers added; "Override" → "Apply discount"; submit payload `tier_override_*` → `sale_discount_*`; sale discount cleared on customer change and on submit success.
- **v2.4** — UI revamp + dark mode (no business-logic changes). See the dedicated **Design system (v2.4)** section in CLAUDE.md for the canonical rules; summary here for the build trail. Brand scale flips teal/navy → amber. Dark mode via a `[data-theme="dark"]` block in `tokens.css` — only semantic tokens flip, raw scales stay constant. `ThemeModeProvider` (`src/lib/themeMode.tsx`) handles `system | light | dark` and writes `data-theme` on `<html>`+`<body>`; `getTheme(direction, mode)` flows the resolved mode into MUI's `palette.mode`. Sun/moon toggle in TopBar. New token family for ambient warmth: `--surface-card` (warm undertone for cards), `--shadow-card` / `--shadow-card-hover` (amber-bloom), `--body-glow` (radial gradient painted via `body::before`). Light-mode primary shifted from brand-700 brown to brand-500 bright amber with dark contrast text. `<Card>` default = no shadow (elevated keeps the bloom). Dropdown overrides centralised in `MuiMenu` / `MuiAutocomplete` / `MuiPopover` so every popup shares one container language. Sidebar active state simplified to an amber pill. Typography unchanged.
- **v2.5** — product detail page + `product_categories` entity. (a) `/products/:id` now renders a `ProductDetailPage` (header, fields, packs, last-10 activity feed); editing happens in a `ProductEditDialog` modal. (b) POS gets a right-side drawer (bottom sheet on mobile) showing the same `ProductDetailBody` plus a sticky "+ Add to cart" footer button — cart state is preserved across drawer open/edit/close. (c) `products.type` (free text) is promoted to a `product_categories` table per shop; `products.category_id` is a NOT NULL FK; unique index swapped from `(shop_id, name, type)` to `(shop_id, name, category_id)`. (d) Product form's Type input is replaced by a `CategoryCombobox` with inline "+ Create new category" dialog. (e) `/products` list gains a URL-synced single-select `?category_id=` filter; the trailing edit/archive icons column is dropped (archive moved into the edit modal's Active toggle); a leading eye-icon column is the explicit "View details" affordance. (f) POS picker gains the same eye icon + "Add to cart" column header + center-aligned [+] button. (g) Three new SECURITY DEFINER RPCs (`search_categories`, `create_category_inline`, `update_category`); `search_products` / `_count` gain `p_category_id`; `create_product_with_opening_stock` prefers `p_category_id` but falls back to `p_type` so stale clients don't break. (h) `products.type` is kept and snapshotted from the category name — drop in a future cleanup migration.
- **v2.6** — foundational variant refactor (SILENT — zero user-visible changes). `products` becomes a template; stock/price/cost moved to a new `product_variants` table; every existing product gets a synthetic default variant carrying the old values. `sale_items` / `purchase_items` / `product_packs` repointed to `variant_id` (with the legacy `product_id` kept in sync via the `sync_product_id_from_variant` trigger). All stock-touching RPCs (`record_sale`, `record_purchase`, `create_product_with_opening_stock`, `search_products`, `define_pack_inline`, `update_pack`, `deactivate_pack`) rewritten to operate on `product_variants`. `search_products` reads through the `product_with_default_variant` compat view so v2.5-era hooks see the same row shape. Six audit queries (§6 of the spec) return zero rows post-migration. Sets up the v2.7 variant-management UI without leaving any "is this a variant product?" branches in legacy code. Migrations 0037–0044.
- **v2.6c** — Stage 2 profit-bug hardening + no-JS-Number-on-money discipline (no user-visible business change). (a) `sale_item_financials` view performs largest-remainder allocation of `invoices.sale_discount_amount` across lines and exposes `line_value` / `line_revenue` / `line_cost` / `line_profit`; `invoice_financials` aggregates per-invoice (`items_subtotal`, `revenue`, `gross_profit`, `gross_margin_percent`, `outstanding`). Both views are `security_invoker = true`. SaleDetail and Dashboard now read the same canonical numbers. (b) `monthly_summary` view rewritten to aggregate from `invoice_financials` rather than recompute. (c) `invoices.outstanding` becomes a generated `numeric(12,2)` column (`greatest(0, total − amount_paid)`) so the sales list reads outstanding credit without JS subtraction. (d) `purchase_item_financials` view exposes per-line `line_subtotal`, `line_overhead` (effective: largest-remainder result or legacy fallback), `line_total`, and `cost_delta`. (e) `daily_sales_7`, `expenses_by_category_mtd`, `total_outstanding` views replace JS reduce calls in reports + dashboard hooks. (f) Three new audit queries (7/8/9 in the suite) — stored-total consistency, financials math sanity, ledger debit ≡ outstanding — return 0 rows. (g) ADRs: profit-bug root cause, single-source-of-truth, audit-suite extension, record_sale transaction integrity (deliberate-failure-verified), cost_at_sale snapshot timing, decimal precision audit, no-JS-Number-on-money discipline. Migrations 0051–0056.
- **v2.7** — variant management UI on top of v2.6. (a) Shop-wide `variant_attributes` + `variant_attribute_values` + `product_variant_attribute_values` tables; new Settings → Variant Attributes page manages them. (b) Product create form gets a "Has variants?" toggle; on, replaces the single price + opening stock fields with a matrix builder. Up to 3 attributes per product (enforced at RPC + UI), cross-product generates combinations as a flat review table with auto-suggested editable SKUs and per-row price overrides. (c) Multi-variant product detail page renders a variants table with per-row inline edit and "+ Add variant" that calls `add_variant_to_product`. (d) Stock-in implements the spec's expanding-line fallback (§7.6) — one variant per stock-in line, not a 2D matrix grid; v2.3 largest-remainder overhead allocation works unchanged. (e) POS multi-variant rows show "X variants" + price range instead of stock/price; "Pick variant" button opens a dialog with click-to-add; cart line + receipt render `"Tracksuit AAA — Red / M"` labels via `product_variant_full.variant_label`. (f) Three new RPCs (`create_product_with_variants`, `add_variant_to_product`, 8 attribute/value CRUD), one new view (`product_variant_full`), `product_with_default_variant` widened with multi-variant aggregates (variant_count, min_price, max_price). Migrations 0045–0048.
- **v2.8** — batch tracking (per-product opt-in via `products.has_batches`). Stock-in captures `batch_no` + manufactured / expiry / `supplier_warranty_days` per stock-in line. Sales use FEFO (First Expire First Out) via `record_sale` — `cost_at_sale` on batched-product rows is the BATCH's `cost_per_unit` (not `variant.avg_cost`) so `invoice_financials.gross_profit` is accurate per batch. A single cart line can produce multiple `sale_items` rows when it spans multiple batches (line_discount_amount split via largest-remainder by qty). Dashboard surfaces `batches_expiring_soon` and `batches_warranty_expiring_soon` views; the widget auto-hides for shops without any batched product. Shop-level alert defaults in Settings (`shops.default_expiry_alert_days` / `default_warranty_alert_days`) with per-product overrides on `products.{expiry_alert_days, warranty_alert_days}`. `deactivate_batch` write-off decrements `variant.stock` directly (temporary — v2.10 inventory_adjustments ledger will replace). `inventory_batches` row is mostly append-only: only `qty_remaining`, `is_active`, `notes` are mutable post-insert (enforced by `batch_immutable_fields` trigger). Migrations 0057–0058. ADRs 2026-05-12-batches-vs-serials-mutually-exclusive, -fefo-override-ux-pick-batch-link, -batch-cost-vs-avg-cost-on-sales, -multi-batch-line-split-data-model, -batch-deactivation-temporary-pattern, -shop-level-alert-defaults-with-product-overrides, -batch-immutability-rules.
- **v2.8.1** — pricing/stock decoupled from product creation. `create_product_with_opening_stock` and `create_product_with_variants` make `p_price`/`p_default_price` nullable, gain `p_has_batches`/`p_expiry_alert_days`/`p_warranty_alert_days`, and raise `cannot_seed_opening_stock_for_batched_product` if has_batches=true AND opening_stock>0. Product create form drops opening-stock + opening-cost inputs; selling price is optional; "Inventory behavior" section (has_batches toggle + per-product alert window overrides) lives on the create form alongside the edit dialog. Variant matrix builder drops the per-row opening-qty column. NewPurchasePage gains a "This is opening stock" checkbox that flips `purchases.is_opening`. ProductDetailBody surfaces a "Selling price not set" banner with a "Set selling price" CTA when `price IS NULL`. POS naturally hides null-price products via the existing 0-stock filter + record_sale's `variant_not_sellable` guard. Migration 0060; ADR `2026-05-12-pricing-decoupled-from-product-creation`.
- **v2.8.2** — partial write-off + auto-deactivate when empty. New RPC `record_partial_writeoff(batch_id, qty, reason)` decrements `variant.stock` and `batch.qty_remaining` by exactly `qty` and appends a dated note; supports RTV / damage / partial-loss workflows without recreating the batch. New BEFORE UPDATE trigger `batch_auto_deactivate_when_empty` fires on the `qty_remaining > 0 → 0` transition and flips `is_active = false` automatically — empty batches stop cluttering the active list. WriteOffBatchDialog gains a "Qty to write off" input that defaults to `qty_remaining`; always routes through `record_partial_writeoff` (the trigger handles the full-batch case). `deactivate_batch` kept for back-compat. New invariant: no batch can have `qty_remaining = 0 AND is_active = true`. Migration 0061; ADR `2026-05-12-partial-writeoff-and-auto-deactivate`.
- **v2.8.3** — visibility fixes for two silent failure modes. (a) `batches_already_expired` view surfaces batches past `expiry_date` with `qty_remaining > 0` — the v2.8 alert widget dropped them the day they expired, allowing silent accumulation. New `ExpiredStockWidget` on the dashboard with danger-tone styling, per-row click navigates to the product detail, "Write off all..." bulk dialog loops `record_partial_writeoff` over each. New route `/inventory/expired` for the full list. (b) `product_with_default_variant` gains `has_null_price_variant`; `search_products` + `_count` gain `p_needs_pricing`. Catalog list renders "Set price ⚠" warnings for null-priced products (single-variant) or "Rs X – set price" for partial multi-variant pricing. URL-synced "Needs pricing" filter chip in the catalog filter bar. Migration 0063; ADRs `2026-05-13-expired-stock-as-separate-dashboard-section`, `2026-05-13-bulk-write-off-loops-existing-rpc`, `2026-05-13-has-null-price-variant-aggregate-on-view`.
- **v2.8.4** — expired-sale policy enforcement. New `expired_sale_policy` enum (`block` / `warn` / `allow`) with shop-level default (`shops.default_expired_sale_policy`, NOT NULL DEFAULT `warn`) and nullable per-product override (`products.expired_sale_policy`; null = use shop default via `coalesce(...)`). `record_sale` enforces in the FEFO loop with a two-pass walk for non-`allow` policies: Pass 1 over non-expired batches (`expiry_date IS NULL OR expiry_date >= current_date`, ASC by expiry); on Pass-1 shortfall, `block` raises `insufficient_non_expired_stock`, `warn` without confirmation raises `expired_stock_needs_confirmation`, `warn` with confirmation runs Pass 2 over expired batches ordered `expiry_date DESC` (least-expired first per spec §3.2) and flags those lines as `sold_expired = true`. Manual-batch override path mirrors the same policy gates. New parameter `p_confirm_expired_sale boolean default false` on `record_sale` (signature change handled via DROP + CREATE in migration 0065). New read-only RPC `preflight_expired_sale_check(p_items jsonb) returns table (variant_id, would_draw_expired, expired_batch_ids, policy)` — gated on `products.has_batches = true` per item; POS calls it before submit to render the block dialog or warn-confirmation dialog up-front instead of a submit-and-fail round-trip. New `sale_items.sold_expired boolean NOT NULL DEFAULT false` column (immutable by the blanket `sale_items_no_modify` trigger; partial index `(sold_expired) WHERE sold_expired`). New opt-in `shops.expired_sale_receipt_disclaimer boolean NOT NULL DEFAULT false` — receipt footer renders the disclaimer text only when the shop has opted in AND the sale has at least one `sold_expired = true` line. UI: settings page subsection (`batches:expired_sales.*`), product edit dialog policy radio (visible only when `has_batches = true`), POS `BlockedExpiredSaleDialog` + `ConfirmExpiredSaleDialog`, sale-detail `Expired stock` badge, dashboard `ExpiredSalesWidget` (auto-hides when count = 0), full audit route at `/inventory/expired-sales`. Migrations 0064 + 0065 + 0066 (preflight ambiguous variant_id fix); ADRs `2026-05-13-expired-sale-policy-three-modes`, `2026-05-13-warn-as-default-policy`, `2026-05-13-preflight-rpc-for-expired-stock-check`, `2026-05-13-sold-expired-flag-snapshotted-not-derived`, `2026-05-13-receipt-disclaimer-opt-in`.
- **v2.8.5** — POS "Pick batch" cart-line picker. Ships the manual-override UI deferred from v2.8 §4.4 (and unblocked by v2.8.4's policy enforcement). `product_with_default_variant` view gains `has_batches` (appended at end of column list because CREATE OR REPLACE can only ADD view columns); `search_products` RETURNS TABLE gains `has_batches boolean` + `default_variant_id uuid` (DROP + CREATE for the signature change). `ProductSearchRow` widens to match. `CartItem` gains `has_batches`, `resolved_variant_id`, `batch_id`, `batch_no`, `batch_expiry_date`; two new reducer actions (`set_batch`, `clear_batch`); `buildItemsPayload()` + preflight items conditionally include `batch_id`. New `PosBatchPicker.tsx` dialog lists `useActiveBatchesForVariant` rows by expiry-ASC, expired rows marked with a red `Badge`, selected row highlighted with a brand "selected" `Badge`, batches with `qty_remaining < line.qty` dimmed + not clickable. **Plain-language relabel**: every user-facing "FEFO" / "oldest" string replaced with "auto-pick" (en) / "خودکار انتخاب" (ur) — shop owners don't speak inventory-jargon. **Auto-pick suggestion badge**: when the cart line has no manual override, the picker computes which batch the server-side auto-pick would draw from (first non-expired with `qty_remaining ≥ line.qty`; falls back to first expired-with-enough-stock for clearance products) and surfaces it with a brand "Will be used" `Badge` + a brand-bordered row + a short info `Banner` at the top ("Auto-pick uses the batch closest to its expiry date so older stock leaves first."). Cart-line affordance "Pick batch" / "Reset to auto-pick" renders only when `has_batches = true`. Backend policy gating in `record_sale` + `preflight_expired_sale_check` is reused unchanged — when an override sets `batch_id`, the manual-override branch runs; otherwise auto-pick. v2.8.4 spec §6.9 / §6.10 can now be exercised from the POS UI instead of direct SQL. Migration 0067; ADR `2026-05-13-pos-batch-picker-ships-as-v285-polish`.
- **v2.9** — server-side permission-based RBAC. 50-permission catalog across 8 categories (sales / products / inventory / customers / suppliers / financial / settings / team). Three system presets (owner / manager / salesperson) as starting templates at invitation time; per-user grants in `user_shop_permissions` table override after. Founding owner has every permission via implicit shortcut in `user_has_permission(shop_id, key)` — `user_shop_access.is_owner = true` short-circuits to TRUE before consulting per-permission rows. 29 declared dependency rules in `permissions_catalog.requires text[]` enforced at grant time AND revoke time (e.g., `record_purchase` requires `view_products, view_product_cost, view_purchases, view_suppliers, view_inventory_batches`; `writeoff_batch` requires `view_inventory_batches, view_batch_cost`). Invitation flow: owner picks preset, the resolved permission set is snapshotted into `pending_invitations.permissions jsonb` (snapshot is authoritative at accept time — no re-resolution against the live catalog), 4-digit verbal confirmation code with 5-strike auto-cancel, 24h expiration. `current_active_shop_id()` reads `request.headers ->> 'app-shop-id'` per call (Supabase pre-request hook NOT used — fallback path adopted because the project is on Free tier and configurability isn't automatable via MCP; the fallback adds ~1ms per call, functionally equivalent). 14 permission-conditional DEFINER views with `with caller_perms as materialized (...)` CTE pattern for per-column projection. 41 existing v2.8.5 RPCs wrapped via rename-and-wrap (`<name>_v28` preserved unchanged; outer wrapper does permission check + delegates + post-delegation audit `_by_user_id` UPDATE). `record_sale` wrapper enforces explicit + implicit discount caps (price=0 vs variant.price is the implicit form), confirm_expired_sale_at_pos permission for warn-policy overrides. `receive_payment` wrapper applies daily non-owner cap with `pg_advisory_xact_lock` serialization. `shop_effective_subscription` view resolves through `shops.owner_user_id` so invited employees inherit the founding owner's subscription state. Two BEFORE-UPDATE trigger gates (`v29_products_archive_gate`, `v29_customers_tier_change_gate`) carve column-level enforcement out of broader edit permissions. Audit query AQ suite extended from 17 to 22 queries (added AQ-13r refined, AQ-14a info / AQ-14b regression split, AQ-18 / AQ-19 / AQ-20 / AQ-21 / AQ-22 for permission integrity). Compressed stabilization replacing 7-day calendar wait: synthetic test pass inside BEGIN..ROLLBACK transaction with transient `current_shop_id()` alias + real non-owner accounts via the invitation flow — 14 boundary tests all PASS. Migrations 0068 through 0080; 20 ADRs filed + 2 bonus (`audit-query-refinements`, `record-sale-permission-enhancements`). Hot-patch chain 0082-0086 shipped same-week under real authenticated traffic: 0082 reverted the profiles half of 0081 (consolidated permissive policy caused 42P17 RLS recursion via `user_shop_access`'s self-querying EXISTS); 0083 dropped `v29_profiles_team_read` entirely (still recursed even on self-read because both permissive policies' USING predicates evaluate); 0084 fixed `current_active_shop_id()`'s fallback-path 42803 grouping bug introduced in 0070; 0085 corrected 0084's `min(uuid)` (not a Postgres built-in) to `array_agg(shop_id)[1]` with count check; 0086 split the audit-column write pattern — non-append-only tables use post-delegation UPDATE (0080), `ledger_entries` uses inline-at-INSERT (the append-only trigger blocks UPDATEs). Client-side UI (Phase I) ships as v2.9.1 (next entry). Production state at server-side shipping: 2 owners, zero invitations issued.
- **v2.9.1** — client-side RBAC frontend on top of v2.9 server. Shipped 2026-05-13 in eight D.11 clusters per the PRD. **Phase C (infrastructure):** migration 0087 mints 11 new RPCs (`update_customer`, `update_product`, `archive_product`, `update_variant_inline`, `update_supplier`, `archive_supplier`, `get_active_shop`, `get_shop_settings`, `list_pending_invitations_for_shop`, `list_permission_audit_for_shop`, `get_team_member_profiles`) + adds `customers.updated_by_user_id` audit column. Migration 0088 mints `get_invitation_for_acceptance` — a pre-shop helper for the invitee's accept page (the invitee has no shop access yet at this point, exempted from AQ-23 P2/P3 gates same as `accept_invitation`/`cancel_invitation`). New client modules: `src/lib/customFetch.ts` injects `app-shop-id` HTTP header from localStorage on every Supabase request; `src/lib/activeShop.ts` is the localStorage transport; `src/lib/permissions.ts` exposes `usePermission(key)`, `useSelfPermissions()`, `useIsOwner()`, plus the typed `PERMISSION_KEYS` constants matching the 50-key catalog exactly; `src/features/team/hooks.ts` adds 11 invitation/team/audit-log mutations + queries; `src/lib/guards.tsx` extends with `RequirePermission`, `RequireAnyPermission`, `RequireOwner`; `src/components/ui/PermissionGated.tsx` + `PermissionTooltip.tsx` implement B.2 hybrid rendering (POS internals HIDE / management CRUD GREY-OUT with `Requires permission: {key}. Contact your shop owner.` tooltip / owner-only settings sections HIDDEN). `src/lib/errorMap.ts` centralises message-text → i18n-key dispatch for the 144 distinct server raise strings — frontend matches by message text (not SQLSTATE) since every controlled raise defaults to P0001 and the `_v28` inner bodies are forbidden from editing per the v2.9 wrap-and-rename rule; see `decisions/2026-05-13-v291-error-dispatch-message-text.md` for the Revisability clause. **Phase D (page-by-page):** 26 routes gated; 7 write hooks migrated to RPCs (`useUpdateCustomer` / `useArchiveProduct` / `useUpdateVariantInline` / `useUpdateSupplier` / `useArchiveSupplier` / `useCreateExpense` / `useUpsertTarget`); 4 new dialogs + 2 new pages — `/settings/team` (TeamMembersTable + PendingInvitationsTable + InviteUserDialog with 4-digit code reveal + EditPermissionsDialog with 50 toggles × 8 collapsible categories × one-level dependency-aware grant/revoke + EditDiscountLimitsDialog + RevokeAccessConfirmDialog + AuditLogSection) and `/invite/accept/:invitation_id` (two-column with shop branding per B.4 — Realities documented in the page header: invitee signs up at `/signup` FIRST since programmatic password-set during accept needs the Supabase admin API, deferred to v2.10; on successful accept the page self-updates `profiles.onboarding_completed = true` via `profiles_self_update` policy so the invitee bypasses the owner onboarding wizard). TopBar gets a ShopSwitcher (visible only when user has 2+ shops, single-shop label otherwise); `useShop()` refactored to call `get_active_shop` RPC instead of the broken-for-non-owners `from('shops').eq('owner_user_id', user.id)` direct query. POS adds `RequirePermission('record_sale')` route guard, hides the "below avg cost" warning when `!view_product_cost`, hides avg_cost in PosVariantPicker, gates the CustomerPicker "+ Add new" affordance on `create_customer_full`, hides phone/address dropdown rows when `!view_customer_contact`, and escalates warn-policy expired-sale lines to the block dialog when the caller lacks `confirm_expired_sale_at_pos` (UX hot-patch for permission-revoked salespersons). **Phase E (testing):** coverage matrix at `audit/2026-05-13-v291-phase-e-coverage-matrix.md` enumerates 250 numbered rows + 6 multi-step flows + 11 halt criteria; owner-runnable synthetic-SQL test bed at `audit/2026-05-13-v291-phase-e-owner-synthetic-test.sql` (1093 lines, runs under real session JWT only). AQ suite extended: AQ-23 exempt list grows from 11 → 14 (adds `get_active_shop`, `get_shop_settings`, `get_invitation_for_acceptance` — all pre-shop or shop-membership-gated helpers); AQ-24 (new) flags legacy `current_shop_id()` callers outside the baseline allowlist (38 `_v28` inner functions + 2 pre-v2.9 views are allowlisted; v2.10 cleanup target for the views). Post-Phase-D, MCP-runnable portion of the suite confirms zero rows across AQ-01..AQ-24 + the `INV-ledger-0086` invariant. **Phase D cluster 6 deferral closed by migration 0089:** extends `update_product` with `is_scan_only` parameter, moves `has_batches` guard rails server-side (`cannot_enable_batches_with_stock` / `cannot_disable_batches_with_active_batches`), syncs legacy `products.type` from category name per ADR-0019, and drops `is_active` from its scope — `is_active` changes now route through `archive_product` (which cascades to all variants). Useful corollary memory rule [[feedback-audit-gaps-block-flag-flip]]: "frontend hooks still on direct-write are release-blocking before any non-owner traffic at a pilot shop." 14 v2.9.1 ADRs filed plus 4 from Phase C. `RBAC_TEAM_UI_ENABLED` env flag flips per-shop manually for the first 1-2 pilot shops via `VITE_RBAC_TEAM_UI_ENABLED=true`; remove flag entirely after 30 days stable per `decisions/2026-05-13-v291-b9-feature-flag-per-shop-manual.md`.

- **v2.9.2** — close cost-column raw-API leaks acknowledged in mig 0091, via DEFINER-ized financial views + column-grant pattern. Shipped 2026-05-13. Three targets across four migrations (0092 → 0095); two more targets deferred to v2.9.3 post-pilot. **NAMING NOTE:** authored under the working label "v2.10a/v2.10b" before the collision with the v2.10 feature ticket (returns/refunds/warranty) was reconciled. Renamed to v2.9.2 per `decisions/2026-05-13-v292-naming-collision-with-returns-feature.md` and the multi-target authorization-discipline ADR `2026-05-13-multi-target-authorization-rule.md`. Production migrations retain v210/v210b in `supabase_migrations.schema_migrations` (rename is source-only). **Target 1 — `sale_items.cost_at_sale` (migs 0092 + 0093):** Migration 0092 DEFINER-izes `sale_item_financials` + `invoice_financials` (from `security_invoker = true` to `false`), adds the v2.9 row-filter (`shop_id = current_active_shop_id() AND (view_all_sales OR cashier_id = auth.uid())`), NULL-projects cost-bearing columns conditionally on view_sale_cost / view_profit_margin. Cascade-recreates `monthly_summary` + `daily_sales_7` unchanged. Migration 0093 applies the column-grant pattern on `public.sale_items` after discovering 0092's column-level `revoke select (cost_at_sale)` was a no-op when the table-level GRANT was still in place — verified by SET ROLE authenticated reading the column without error. Correct pattern: REVOKE SELECT ON sale_items FROM authenticated + anon, then GRANT SELECT (every column except cost_at_sale) ON sale_items TO authenticated. Hook + component refactor in `src/features/sales/`: `useSale` drops `cost_at_sale` from nested sale_items SELECT and reads via `sale_item_financials.cost_at_sale`; SaleDetailItem cost/profit fields widened to `number | null`. **Target 2 — `customers.outstanding_balance` (mig 0094):** same column-grant pattern; `customers_view` (DEFINER, mig 0074) already projects outstanding_balance conditionally on view_customer_outstanding, so no view changes needed. `useCustomer` / `useCustomers` / `useCreateCustomer` migrated from `.from('customers').select('*')` to `.from('customers_view')`; Customer type widened. **Target 3 — `inventory_batches.cost_per_unit` (mig 0095):** same column-grant pattern; `inventory_batches_view` (DEFINER, mig 0074) already projects conditional on view_batch_cost. `useActiveBatchesForVariant` + `useAllBatchesForVariant` migrated to `inventory_batches_view`; BatchPickerRow.cost_per_unit widened to `number | null`. **Verified live:** SET ROLE authenticated; SELECT each cost column → 42501 permission denied. Mig 0091's permissive row-read policies remain in place — harmlessly superseded by the DEFINER-view path. Two new `security_definer_view` advisor entries expected (sale_item_financials + invoice_financials); add to baseline. **Targets 4 + 5 deferred to v2.9.3 post-pilot:** products/variants cost columns (`cost, avg_cost, last_purchase_cost`) require DEFINER-izing `product_with_default_variant`; purchase_items cost columns require DEFINER-izing `purchase_item_financials`. Both add advisor surface and deserve pilot signal before adding complexity. ADR `2026-05-13-v292-definerize-financials-and-column-revoke`. **Maintenance contract:** any future migration that adds a column to `public.sale_items`, `public.customers`, or `public.inventory_batches` MUST also `grant select (<new_col>) on public.<table> to authenticated;` or the column is invisible to the application — mig 0093/0094/0095 headers document the current baselines.
