import { Box } from '@mui/material'
import styles from './EmailVerification.module.scss'

const EmailVerification = () => {
  return <Box className={styles.root}>EmailVerification</Box>
}

export default EmailVerification
